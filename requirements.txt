# ══════════════════════════════════════════════════════════════════════════
# requirements.txt  —  Bridging the Post-Disaster Imagery Gap
# Revised methodology: U-Net + SegFormer-B2 + ChangeFormer
# ══════════════════════════════════════════════════════════════════════════

# ── Core Deep Learning ────────────────────────────────────────────────────
torch>=2.0.0
torchvision>=0.15.0

# ── Data Processing & Scientific Computing ────────────────────────────────
numpy>=1.24.0
scipy>=1.10.0          # spearmanr (13_sample_size), kruskal (14_disaster_type)
Pillow>=9.0.0

# ── Machine Learning & Metrics ────────────────────────────────────────────
scikit-learn>=1.2.0    # jaccard_score, f1_score, etc.
statsmodels>=0.14.0    # OLS regression in scripts 13 and 14
pandas>=2.0.0          # DataFrame support in 14_disaster_type_analysis.py

# ── Transformer Models (SegFormer-B2) ─────────────────────────────────────
transformers>=4.35.0   # HuggingFace: nvidia/mit-b2 backbone for SegFormer

# ── FID / IS computation (Study 1 — DisasterGAN quality) ─────────────────
pytorch-fid>=0.3.0     # InceptionV3-based FID (04b_compute_fid_is.py)

# ── Visualization ─────────────────────────────────────────────────────────
matplotlib>=3.7.0

# ── Progress & Utilities ──────────────────────────────────────────────────
tqdm>=4.65.0

# ── Dataset Download ──────────────────────────────────────────────────────
kaggle>=1.5.16

# ══════════════════════════════════════════════════════════════════════════
# INSTALLATION
# ──────────────────────────────────────────────────────────────────────────
# GPU (CUDA 11.8):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#
# Then install all other deps:
#   pip install -r requirements.txt
# ══════════════════════════════════════════════════════════════════════════
